"""
stilltent memory subsystem.

Provides pluggable memory backends (mem9, supermemory) and the ASMR
agentic layer that wraps them with parallel observer/searcher/ensemble agents.

Primary entry point:
    from memory.asmr import MemoryRouter
"""
