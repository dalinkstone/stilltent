# agent-openclaw.yml — OpenClaw agent runtime overlay
version: "3.8"

services:
  openclaw:
    build:
      context: ../../config/agents/openclaw
      dockerfile: Dockerfile
    env_file:
      - ../../.env
    depends_on:
      - mnemo-server
    networks:
      - stilltent
    restart: unless-stopped
