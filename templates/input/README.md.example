# <project-name>

<One paragraph describing what this project does. This becomes the agent's
primary understanding of the project. Be specific about the problem being
solved, the target users, and the key value proposition.>

## Goals
- <What the project should do — each goal becomes an item the agent works toward>
- <Key features the project must have>
- <Behaviors or capabilities that define "done">

## Non-Goals
- <What the project should NOT do — scope boundaries>
- <Features that are explicitly out of scope>
- <Things the agent should not spend time on>

## Tech Stack (optional)
- <Language/framework preferences — if omitted, the agent detects from repo files>
- <Key dependencies or libraries to use>

## Architecture (optional)
- <Any architectural constraints or preferences>
- <How the project should be structured>
- <Deployment model or runtime requirements>

## Testing (optional)
- <How to run tests — if omitted, the agent auto-detects from project files>
- <Any testing conventions or requirements>
